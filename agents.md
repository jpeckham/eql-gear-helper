# Agents

## Coding workflow

- Always run `dotnet build` after code changes.
- Always run `dotnet test` after successful build to validate behavior.
- Do not report completion of implementation until both commands have been executed and passed (or clearly explain why tests cannot run).

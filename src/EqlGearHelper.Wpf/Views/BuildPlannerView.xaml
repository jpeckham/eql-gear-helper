<UserControl x:Class="EqlGearHelper.Wpf.Views.BuildPlannerView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"><Grid Margin="8"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><StackPanel><TextBlock Text="Build Planner" FontSize="24" FontWeight="Bold"/><TextBlock Text="{Binding Status}" AutomationProperties.Name="Build planner status" AutomationProperties.LiveSetting="Polite"/></StackPanel><StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12"><ComboBox Width="140" ItemsSource="{Binding Classes}" SelectedItem="{Binding ClassOne}" AutomationProperties.Name="First class"/><ComboBox Width="140" Margin="8,0" ItemsSource="{Binding Classes}" SelectedItem="{Binding ClassTwo}" AutomationProperties.Name="Second class"/><ComboBox Width="140" ItemsSource="{Binding Classes}" SelectedItem="{Binding ClassThree}" AutomationProperties.Name="Third class"/><Button Content="Build set" Command="{Binding BuildSetCommand}" Margin="12,0,0,0"/><Button Content="Compare" Command="{Binding CompareCommand}" Margin="8,0"/><Button Content="Cancel" Command="{Binding CancelCommand}" Margin="8,0"/></StackPanel><Grid Grid.Row="2"><Grid.ColumnDefinitions><ColumnDefinition Width="220"/><ColumnDefinition Width="*"/><ColumnDefinition Width="300"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Requirements" FontWeight="Bold"/><ListBox ItemsSource="{Binding Requirements}"/></StackPanel><DataGrid Grid.Column="1" Margin="8,0" ItemsSource="{Binding Loadout}" AutoGenerateColumns="True" IsReadOnly="True"/><StackPanel Grid.Column="2"><TextBlock Text="Selected-slot gaps" FontWeight="Bold"/><ListBox ItemsSource="{Binding Gaps}" SelectedItem="{Binding SelectedGap}" DisplayMemberPath="Slot"/><TextBlock Text="{Binding SelectedGap.MissingEffects}" TextWrapping="Wrap"/></StackPanel></Grid></Grid></UserControl>
